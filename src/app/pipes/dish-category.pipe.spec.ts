import { DishCategoryPipe } from './dish-category.pipe';

describe('DishCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new DishCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
