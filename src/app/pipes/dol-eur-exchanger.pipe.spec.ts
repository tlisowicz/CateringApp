import { DolEurExchangerPipe } from './dol-eur-exchanger.pipe';

describe('DolEurExchangerPipe', () => {
  it('create an instance', () => {
    const pipe = new DolEurExchangerPipe();
    expect(pipe).toBeTruthy();
  });
});
