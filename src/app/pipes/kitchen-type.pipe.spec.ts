import { KitchenTypePipe } from './kitchen-type.pipe';

describe('KitchenTypePipe', () => {
  it('create an instance', () => {
    const pipe = new KitchenTypePipe();
    expect(pipe).toBeTruthy();
  });
});
